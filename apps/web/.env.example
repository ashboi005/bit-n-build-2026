# Copy to .env and fill in. In Coolify, paste these as environment variables.

NODE_ENV=development

# Public URL of the SERVER app. Local: http://localhost:3000
# In production this must be the real server domain, reachable from the browser.
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
